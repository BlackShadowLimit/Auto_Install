### 基本語言與國家
d-i debian-installer/language string en
d-i debian-installer/country string TW
d-i debian-installer/locale string en_US.UTF-8
d-i console-setup/layoutcode string us

### 網路
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-auto
d-i netcfg/get_domain string unassigned-domain

### 時區
d-i time/zone string Asia/Taipei
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean true

### 分割與 LVM (/ 與 /home 獨立)
d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select lvmhome

d-i partman-auto/expert_recipe string \
  lvmhome :: \
    1000 20000 -1 ext4 \
      $primary{ } $bootable{ } method{ lvm } \
    . \
    500 20000 -1 ext4 \
      lv_name{ root } in_vg{ vg0 } method{ format } format{ } \
      use_filesystem{ } filesystem{ ext4 } mountpoint{ / } \
    . \
    500 20000 -1 ext4 \
      lv_name{ home } in_vg{ vg0 } method{ format } format{ } \
      use_filesystem{ } filesystem{ ext4 } mountpoint{ /home } \
    . \
    512 4000 8000 linux-swap \
      lv_name{ swap } in_vg{ vg0 } method{ swap } format{ } \
    .

d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### 軟體套件
tasksel tasksel/first multiselect standard
d-i pkgsel/include string sudo vim git

### mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### 安裝結束
d-i grub-installer/only_debian boolean true
d-i finish-install/reboot_in_progress note
